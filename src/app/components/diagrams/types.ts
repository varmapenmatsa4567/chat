// Frontend-facing diagram types. The canonical definitions live in
// src/lib/diagram.ts (shared with the agent tool); re-export here for the UI.
export type {
  MermaidDiagram,
  MermaidDiagramType,
} from "../../../lib/diagram";
